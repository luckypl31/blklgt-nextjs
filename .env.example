# Firebase — same project as iamwesmiller.com so film data is shared.
# These are public-by-design (they identify the project, they don't grant access).
# Access is controlled by firestore.rules / storage.rules.
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyAdDebaGVZ6GkK31rHAJRALJZY7HLCBXSQ
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=iamwesmiller-270ac.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=iamwesmiller-270ac
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=iamwesmiller-270ac.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=200074378413

# blklgt.com's own web app registration. Create it in
# Firebase console > Project settings > Your apps > Add app > Web.
# NOTE: this is only used to identify the client. It is deliberately NOT part
# of any Firestore path — that was the bug in the old setup.
NEXT_PUBLIC_FIREBASE_APP_ID=REPLACE_ME

NEXT_PUBLIC_SITE_URL=https://www.blklgt.com
